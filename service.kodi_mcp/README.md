# Legacy Bridge Addon Stub

This directory is not the source of truth for `service.kodi_mcp`.

The maintained Kodi bridge addon source lives in the standalone repo:

`/srv/openclaw-projects/kodi_mcp_addon/workspace/project/packages/service.kodi_mcp`

The GitHub remote for that project is:

`git@github.com:kcook98765/kodi_mcp_addon.git`

Keep server-side bridge client code, tests, and docs in this repo. Keep Kodi-resident addon code in the standalone addon repo.
